# SmartDeviceAI Database

SQLite runtime database:

- `smartdeviceai.db` is created automatically by backend service code.
- Table `inference_logs` stores detection and explanation results for API calls.

